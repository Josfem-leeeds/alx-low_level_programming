#include <stdio.h>
/**
 * main - this is where the code starts from
 *
 * Return: this returns zero always
 */
int main(void)
{
	int numz;

	for (numz = 48; numz <= 57; numz++)
		putchar(numz);
	putchar('\n');
	return (0);
}
