#include "main.h"

/**
 * _abs - this gives the absolute value of the function
 * @x: parameter
 * Return: This returns and integer value
 */
int _abs(int x)
{
	if (x < 0)
	{
		x = (-1) * x;
	}
	return (x);
}

