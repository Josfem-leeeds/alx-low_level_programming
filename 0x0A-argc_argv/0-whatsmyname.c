#include <stdio.h>
/**
 * main - the main function that receives the arguments
 * @argc: the argument counter
 * @argv: the argument strings
 * Return: Returns and integer value on success
 */
int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	return(0);
}
