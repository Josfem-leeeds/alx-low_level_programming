#include <stdio.h>

/**
 * main -Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char *p = "with proper grammar, but the outcome is a piece of art,";

printf("%s", p);
return (0);

}
